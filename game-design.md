# Magical Girl versus Sharkfall(箒シューティング企画書 / シルバーウィーク バイブコーディング)

GitHubリポジトリ名: `magical-girl-versus-sharkfall`

## 概要

XREAL Air 2 Pro + XREAL Beam Pro を使ったARゲーム。プレイヤーは箒に乗って手続き生成された都市(ビル群)上空を飛び回り、街に出現する空飛ぶサメをジェスチャー魔法で撃退する。

コア体験は基本的にVR的な没入型フライトシューティングとして割り切り、AR的な演出(タイトル/ポーズ/リザルト画面での空間固定表示など)は本編の作り込みが終わったあとの拡張枠として扱う。

## 使用機材・技術スタック

- XREAL Air 2 Pro(3DoF頭部トラッキングのみ。位置トラッキング・ハンドトラッキング・視線トラッキングは非対応)
- XREAL Beam Pro(8+256GB、Android 14ベースのNebulaOS。Unityアプリをネイティブ実行する母艦)
- Unity(XREAL SDK 3.0 / XR Plugin経由で頭部トラッキング取得)
- 舞台データ: 手続き型都市生成(道路網・区画分割・建物配置をUnity非依存ロジックで自動生成。実在都市データは使わない)
- エディタ: Zed + Claude Code
- 開発環境: 現状はWindows上で直接開発(Unity Editor、Zed、Claude Codeすべてネイティブ実行)
  - コンテナ化(Podman等)は将来的な検討事項として保留中、今は導入していない
  - 実機(Beam Pro)への転送・デバッグはadb over Wi-Fi推奨(USBパススルーの煩雑さを回避)

## 開発ワークフロー

- AGENTS.md(Zed/Claude Code双方で使える共通規格)をルート+主要ディレクトリごとに配置し、Claude Codeへの「丸投げ」開発をしやすくする
  - ルート: プロジェクト概要、ビルド/実行コマンド
  - Assets/Scripts/: ゲームロジックの規約
  - Assets/Scripts/Core/City/: 都市生成ロジック(シード・パラメータの扱い方)の規約
- Claude Codeのサブエージェント(.claude/agents/*.md)で役割分担:
  - gameplay-coder: 飛行制御・シューティングロジック(Assets/Scripts担当)
  - city-generation: 手続き型都市生成・コリジョン設定
  - code-reviewer: レビュー専用(read-only)
- Unityのシーン/プレハブはYAMLマージ事故が起きやすいため、git worktreeでの並列セッションを使う場合は「同じシーンを同時にいじらない」ように作業単位を切る

## 操縦(Beam Proの傾きで制御。タッチパッドは攻撃用に温存)

- **ロール(左右傾き)の通常域** → 旋回・バンク
- **ピッチ(前後傾き)の通常域**
  - 機首を下げる → 降下+加速
  - 機首を上げる → 上昇+減速
- **ブレーキ(メイン案)**: ピッチを可動域の限界まで引き切る(機首を目一杯上げる)→ 失速ブレーキ
- **ブレーキ(サブ案)**: ロールを極端(タッチパネル面がほぼ地面に垂直)まで倒す → スキー止め的な急減速。今回は見送り、後回しの拡張候補
- 実装メモ:
  - ニュートラル位置は起動時にキャリブレーションして基準を取る
  - 傾き角度の小さい範囲はデッドゾーンとして無視
  - 感度カーブは非線形(緩め始めて後半で効きが強くなる)を検討
  - 通常域と極端域(ブレーキ)の間には緩衝ゾーンを設け、誤爆を防ぐ

## 街: 手続き型都市生成

実在都市データ(PLATEAU等)は使わず、Unity非依存ロジックで街を自動生成する。上空を飛び回る
フライトシューティングのため、地上レベルのディテールよりもシルエット・スカイラインの見え方を
優先する。

- **道路網**: グリッド(碁盤の目)状に生成。有機的な道路網(L-system等)は拡張候補
- **区画分割**: 道路で囲われた矩形エリアを敷地単位に分割し、建物を配置する
- **建物**: 敷地ごとに直方体ベースの建物を生成
  - 高さ・幅・奥行きをランダム化。ノイズ(パーリン等)で「都心部は高層、郊外は低層」という
    自然なスカイラインの起伏をつける
  - 直方体1種類だけでなく、階段状(ウェディングケーキ型)・L字型など2〜3種類の形状バリエーションを
    混ぜて単調さを減らす
  - コライダーは単純な`BoxCollider`で済ませ、複雑なメッシュコライダーは使わない
- **ランドマークタワー**: 街の中心に、ひときわ高い塔を1つ配置する。ボス戦の舞台になる
- **シード**: 生成シードを固定すればいつも同じ街に、ランダムにすれば毎回違う街になる。両対応にする
- **パフォーマンス**: Beam Proの性能に合わせて、生成するブロック数・ポリゴン数を
  パラメータで調整できるようにする

## 攻撃: ジェスチャー魔法(Beam Proのタッチパッドをなぞる)

「魔法の杖を振る」イメージで、なぞり方によって攻撃内容が変わる。

- **直線に素早くフリック** → 火球(直進弾)
- **円を描く** → バリア展開/範囲攻撃(防御的にも使える)
- **ジグザグになぞる** → 雷撃(複数の敵にチェイン)
- **長押し(ホールド)** → チャージして大技、離した瞬間に発射

判定方式は軌跡の形状認識(テンプレートマッチング、$1 Unistroke Recognizer的なアプローチ)を想定。タッチ開始で詠唱中演出、終了時に軌跡判定→攻撃発動、テンプレート不一致なら詠唱失敗演出。

## 敵: 空飛ぶサメ

- **スクール(小型・群れ)**: 一直線に編隊で突っ込んでくる。ジグザグ(雷)のチェイン処刑向き
- **追尾(中型)**: プレイヤーを追いかけて体当たり。直線フリック(火球)で正面迎撃
- **ボス(大型)**: 街の中心に生成される巨大なランドマークタワーに絡みつく演出。詳細は下記

突進(高速で助走してから直線的に突っ込むタイプ)は今回は見送り。

## ウェーブ制

- 敵タイプは「スクール」「追尾」「ボス」の3種類で完結させる
- ウェーブ1: スクールのみ、少数
- ウェーブ2: スクール+追尾を混ぜる(敵タイプの組み合わせ変更はここで一度だけ使用)
- ウェーブ3以降: 敵タイプの組み合わせは固定し、以下の2軸だけで難易度を上げる
  - 出現数(スクール/追尾それぞれ、ウェーブ数に比例して増加)
  - ステータス倍率(HP・速度。ウェーブ数×係数、ボスにも同じ係数を適用)
- 数ウェーブに1回、ボス単体のウェーブを挟む(雑魚なしでボスに集中)

## ボスの行動パターン(フェーズ制)

HPを3段階に区切り、削れるごとに行動が変化する。

- **突進ダイブ**: 助走モーションのあと一直線に突進。直線フリックで正面迎撃、またはチルトで回避
- **衝撃波/咆哮**: その場で周囲に円状の衝撃波。円(バリア)ジェスチャーで防御しないと被弾
- **雑魚召喚**: スクールを呼び出して数で押してくる。ジグザグ(雷)での掃討向き
- **弱点露出**: 咆哮直後などに隙ができ、ホールドチャージの大技を叩き込むウィンドウになる

フェーズ進行:
1. フェーズ1(HP高): 突進ダイブと衝撃波を交互に
2. フェーズ2(HP中): 雑魚召喚が追加され、同時対応が必要に
3. フェーズ3(HP低): 全パターンがランダム・高頻度化。弱点露出の頻度も上げる

拡張候補(時間が余れば): ボスがランドマークに体当たりして瓦礫をばら撒く攻撃パターン。

## AR要素(隙を見て追加する拡張枠、コア設計には手を入れない)

Air 2 Proは3DoF回転のみで位置・ハンドトラッキングがないため、実物体への正確な重ね合わせはできない。「その場に貼り付ける」レベルのBody Anchorモードで以下を検討:

- タイトル画面: 部屋の中に生成された都市のミニチュア(ジオラマ)が浮かび、箒が刺さっている演出
- ポーズメニュー: 一時停止した瞬間だけ部屋に固定表示するメニュー
- リザルト画面: 壁に張り付くようなスコアボード表示

いずれもゲームプレイ中のロジックには影響しないため、コア部分完成後の後付けで対応可能。

## 実装優先順位(叩き台)

1. 頭部トラッキング(Air 2 Pro)とBeam Proの傾き取得、疑似飛行コントローラー
2. 手続き型都市生成(道路網・区画分割・建物配置)とコリジョン設定、飛行ルートの調整
3. タッチジェスチャー判定(まずは直線フリックと円の2種類)とシューティング基本ループ
4. 敵AI(スクール/追尾)とウェーブ管理(出現数・ステータス倍率のパラメータ化)
5. ボスのフェーズ制行動パターン
6. 仕上げ(効果音・UI・難易度調整・実機での操作感チューニング)
7. 余裕があればAR演出(タイトル/ポーズ/リザルトの空間固定)、ランドマーク防衛要素、スキー止めブレーキ
